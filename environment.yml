name: vol-surface-project
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - pip:
    - yfinance
  - numpy
  - pandas
  - matplotlib
  - jupyter
  - plotly
  - python = 3.13
  # Add other dependencies as needed